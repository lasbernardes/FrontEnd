import { AnimacaoDirective } from './animacao.directive';

describe('AnimacaoDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimacaoDirective();
    expect(directive).toBeTruthy();
  });
});
